import React, { PropTypes } from 'react'
import '../sass/header.sass'

let Header = ( ) => {

  return (
    <div className='header'>
    	<h2>Header</h2>
    </div>
  )
}

Header.propTypes = {
}

export default Header