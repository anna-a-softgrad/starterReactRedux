import React, { PropTypes } from 'react'
import '../sass/footer.sass'

let Footer = ( ) => {

  return (
    <div className='footer'>
    	<h2>Footer</h2>
    </div>
  )
}

Footer.propTypes = {
}

export default Footer